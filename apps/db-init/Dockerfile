# This file is generated by Nx.
# Build the docker image with `npx nx docker:build @gql-cms/db-init`.
# Tip: Modify "docker:build" options in project.json to change docker build args.
#
# Run the container with `nx docker:run @gql-cms/db-init -p 3000:3000`.

FROM docker.io/node:lts-alpine
LABEL description="Synthetic Persona seeding and GraphQL API for PostgreSQL database https://github.com/graphile/postgraphile"

ENV HOST=0.0.0.0
ENV PORT=3000
EXPOSE 3000

WORKDIR /app

COPY dist .
COPY package.json .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.

RUN npm --omit=dev -f install

CMD [ "node", "main.js" ]
