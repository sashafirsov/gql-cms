# Environment Configuration Example
# Copy this file to .env and configure with your settings
# .env is in .gitignore and should never be committed

# Database Connection
DATABASE_URL=postgresql://postgres:password@localhost:5432/gql_cms

# or DATABASE_URL=postgres://postgres:change_me@gql-cms-db:5432/forum_example

# Node Environment
NODE_ENV=development

# Application Ports
PORT=3000

# JWT Keys for Authentication (Auto-generated by bin/jwt_keys.sh during build)
# These will be automatically populated when you run: npm run build
# Or manually generate with: bash bin/jwt_keys.sh
JWT_PRIVATE_KEY=""
JWT_PUBLIC_KEY=""

# Optional: OAuth Provider Configuration (for future OAuth support)
# OIDC_GOOGLE_ISSUER=https://accounts.google.com
# OIDC_GOOGLE_CLIENT_ID=
# OIDC_GOOGLE_CLIENT_SECRET=
# OIDC_GITHUB_CLIENT_ID=
# OIDC_GITHUB_CLIENT_SECRET=
