# Environment Configuration Example
# Copy this file to .env and configure with your settings
# .env is in .gitignore and should never be committed

# DB
# Parameters used by db container
POSTGRES_DB=gql_cms
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me

# GRAPHQL
# For Docker: use gql-cms-db (Docker service name)
# For local development: use localhost
# Choose the appropriate DATABASE_URL based on how you're running the services
#
# Note: All schemas (gql_cms, northwind, acl) live in the SAME database
# - acl schema: Authentication/authorization (shared by all)
# - northwind schema: Northwind sample data
# - gql_cms schema: CMS data

# Option 1: Running in Docker (npm start / docker-compose)
DATABASE_URL=postgres://postgres:change_me@gql-cms-db:5432/gql_cms

# Option 2: Running locally (nx serve gql-api with local PostgreSQL)
# DATABASE_URL=postgres://postgres:change_me@localhost:5432/gql_cms

# Node Environment
NODE_ENV=development

# Application Ports
PORT=3000

# JWT Keys for Authentication (Auto-generated by bin/jwt_keys.sh during build)
# These will be automatically populated when you run: npm run build
# Or manually generate with: bash bin/jwt_keys.sh
JWT_PRIVATE_KEY=""
JWT_PUBLIC_KEY=""

# Optional: OAuth Provider Configuration (for future OAuth support)
# OIDC_GOOGLE_ISSUER=https://accounts.google.com
# OIDC_GOOGLE_CLIENT_ID=
# OIDC_GOOGLE_CLIENT_SECRET=
# OIDC_GITHUB_CLIENT_ID=
# OIDC_GITHUB_CLIENT_SECRET=
